-if user_signed_in?
  %li=link_to icon(:gears, 'administracja'), admin_root_path if can? :manage, :backend
  %li=link_to icon(:user, 'moje konto'), account_root_path   if can? :manage, :own_account
  %li=link_to icon(:'sign-out', 'wyloguj'), destroy_user_session_path, method: :delete
-else
  %li=link_to icon(:users, 'rejestracja'), new_user_registration_path
  %li=link_to icon(:'sign-in', 'zaloguj'), new_user_session_path
